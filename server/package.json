{
  "name": "server",
  "version": "1.0.0",
  "description": "Bun + Hono + Python Backend",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "hono": "^4.0.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
